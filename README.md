ecommerce backend server side
commands to run project 
1.clone the project through git repo link
2.install the dependencies by using command : npm install
3.to start the project run : npm run server
           